@model IEnumerable<Test.Models.PayrollCust>

@{
    ViewBag.Title = "Index";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table table-hover text-white table-bordered">
    <tr class="table-primary text-white">
        <th>
            @Html.DisplayNameFor(model => model.prCust_paymnt)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.prCust_date)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Customer.Cust_Firstname)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Organization.org_name)
        </th>
        <th></th>
    </tr>

    @foreach (var item in Model)
    {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.prCust_paymnt)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.prCust_date)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Customer.Cust_Firstname)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Organization.org_name)
            </td>
            <td>
                @Html.ActionLink("Edit", "Edit", new { id = item.prCust_id }) |
                @Html.ActionLink("Details", "Details", new { id = item.prCust_id }) |
                @Html.ActionLink("Delete", "Delete", new { id = item.prCust_id })
            </td>
        </tr>
    }

</table>
